# 任务清单: 面板美化（cladd 风扁平主题 + 中文标签）

目录: `helloagents/plan/202609190930_panel-theme/`

参考：demo.qqq.bot = React + Tailwind v4.3.1 + 自定义 `--cladd-*` 设计令牌（多级灰、outline、primary、radius、自动深/浅色）+ lucide 图标。本包**只借设计语言**，不引入 Tailwind/构建/依赖。

---

## 1. 设计令牌与主题
- [√] 1.1 `src/scan/html.ts` 内联 CSS 重写为令牌驱动：背景/前景多级灰、outline、primary、语义色（ok/warn/danger/info）、radius；`prefers-color-scheme` 自动深/浅色
- [√] 1.2 系统字体栈、`tabular-nums` 数字对齐、粘性表头与粘性"名称"列、斑马纹与 hover、横向滚动容器

## 2. 布局与组件
- [√] 2.1 顶部摘要卡：各阶段数量、免费数、队列中数量、最近开售倒计时
- [√] 2.2 表格压缩为 13 个核心列 + 展开行（明细含：阶段拆分、预售、备注/风险、等级轨迹、执行、24/72 时净值、slug/owner、链接）
- [√] 2.3 徽标组件：等级（A/B/C/D）、阶段（未开售/新开售/在售/陈旧/售罄/已结束/待复审）、免费、预售、陈旧；已铸用进度条；速度/售罄预计色阶；净值红绿
- [√] 2.4 行点击展开/收起（链接与勾选不触发），移动端卡片化

## 3. 中文标签
- [√] 3.1 所有表头、筛选器、预设按钮、状态条、隐藏计数、摘要卡中文化；阶段值中文化
- [√] 3.2 标识符保持原文：名称、slug、合约地址、链 key、txHash、金额单位；CLI 保持英文（仓库约定）

## 4. 测试与文档
- [√] 4.1 更新 `tests/dashboard.cjs`：表头数 = 主行单元格数；每个 `data-sort` 键都有对应 data 属性；FREE/免费、slug?、阶段属性、预设 id、净值断言适配新结构
- [√] 4.2 `npm run build` 与 `node --test tests/*.cjs` 通过（当前 86 例不回归）
- [√] 4.3 真链渲染检查（`--report` 生成后检查关键结构与中文标签）；更新 `README.md`（面板说明）与 `CHANGELOG.md`

---

## 执行总结

**结果:** 11/11 完成。`npm run build` 通过，`node --test tests/*.cjs` **86/86**。

**真实渲染检查（`--report`，4 个真实目标）：** 21.9KB 单文件、`lang="zh-CN"`、13 个表头 = 主行 13 个单元格、4 行明细、4 个摘要卡、阶段徽标与进度条齐全、短名单 DOM 在脚本之前（IIFE 可正常绑定）、**0 个外部资源**（脚本/样式/图片全部内联）。

**取舍说明:** 只借 demo.qqq.bot 的设计语言（Tailwind v4 + `--cladd-*` 令牌的扁平风格），未引入 Tailwind/构建/前端框架——保持单进程零依赖与离线可用。若后续仍需频繁调整样式，可再评估预构建 Tailwind 单文件（方案 B）。
