node_modules/
dist/
*.log

# Secrets — never commit
.env
.env.*
!.env.example

# Batch config that may embed RPC keys — keep it local
targets.local.json

# Audit scan cache — rebuilt on demand
.audit-cache/

# Private keys — never commit
wallets/
keys.txt
*.key
