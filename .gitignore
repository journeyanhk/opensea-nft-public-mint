node_modules/
dist/
*.log

# Secrets — never commit
.env
.env.*
!.env.example

# Batch config that may embed RPC keys — keep it local
targets.local.json

# Private keys — never commit
wallets/
keys.txt
*.key
