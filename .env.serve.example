# ============================================
# 服务模式（--serve）环境变量 — 复制为 .env.serve
# ============================================
# 重要：本文件绝不能出现 PRIVATE_KEY / PRIVATE_KEYS。
# --serve 启动时会断言环境中没有私钥，检测到即拒绝启动。
# 执行 mint 仍在另一个终端用 .env（含私钥）跑 --batch。

SERVE_HOST=127.0.0.1
SERVE_PORT=8787

# 调度
SCAN_CHAINS=robinhood,arc
SCAN_INTERVAL_MIN=15
SCAN_LIMIT=20
SCAN_LOOKBACK_DAYS=0.5
SCAN_HORIZON_HOURS=72
SCAN_INCLUDE_MINTS=0

# 每轮扫描后自动补几个目标的状态（owner/社交/缩略图）；0 = 关闭，改为手动跑 --refresh-targets。
# 有了它，首次迁移不需要停服：服务会在若干轮内自行消化积压。
REFRESH_PER_TICK=20

# RPC：eth_call 用（可留空走公共节点）
RPC_URL_ROBINHOOD=
RPC_URL_ARC=

# 扫描专用（宽 eth_getLogs 范围的付费节点时填写；留空自动用公共端点）
SCAN_RPC_URL_ROBINHOOD=
SCAN_RPC_URL_ARC=

# OpenSea（可选：地板价 stats 与合约→slug 反查）
OPENSEA_API_KEY=

# OpenSea 请求上限（每秒）；默认 2。429 会读 retry-after 退避重试一次。
# 免费 key 建议 1-2，付费 key 可调高。
OPENSEA_RPS=2

# X（Twitter）粉丝数（可选，默认关闭）；设为 1 时随 refresh 抓取并缓存 24h
ENABLE_X_METRICS=
